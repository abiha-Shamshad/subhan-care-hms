# Unrelated local tooling/artifacts at the workspace root — not part of this project
.claude/
.agents/
screenshots/
screenshots.zip

# Backend
backend/node_modules
backend/.env
backend/npm-debug.log*

# Frontend (frontend/.gitignore also covers this, kept here for safety with root-level tooling)
frontend/node_modules
frontend/dist
